# Project working guidance

For requests about MuJoCo simulation, the live simulation panel, scene assets,
camera controls, or the Unity-like scene editor, read `SIMULATION_SCOPE.md`
before inspecting code. Start with the listed primary files and avoid unrelated
subsystems unless the request explicitly changes focus or a direct dependency
requires them.

